language: python

os:
  - linux

python:
  - "3.5"
  - "3.6"

before_install:
  - "pip install -U pip"

# command to install dependencies
install:
  - python -m pip install .

# command to run tests
script:
  - pytest -v test_v2/ --cov=cantherm/core/